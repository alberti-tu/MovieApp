import { colors } from "../styles/theme"

export const navigationHeaderOptions: any = {
  headerStyle: {
    backgroundColor: colors.primary,
  },
  headerTintColor: colors.secondaryText,
  headerTitleStyle: {
    fontWeight: 'bold',
  },
}