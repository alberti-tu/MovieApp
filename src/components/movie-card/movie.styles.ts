import { StyleSheet } from "react-native"

export const movieCardStyles = StyleSheet.create({
  container: {
    margin: 4,
  },
  posterImage: {
    borderRadius: 12,
    height: 128 * 3 / 2,
    width: 128,
  }
})