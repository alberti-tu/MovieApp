import { StyleSheet } from "react-native"

export const movieCardStyles = StyleSheet.create({
  container: {
    margin: 4,
    width: 128,
  },
  posterImage: {
    borderRadius: 12,
    aspectRatio: 2/3,
    width: '100%'
  }
})