import { StyleSheet } from "react-native"

export const movieStyles = StyleSheet.create({
  container: {
    margin: 4,
  },
  posterImage: {
    borderRadius: 12,
    width: 128,
    height: 128 * 3 / 2
  }
})